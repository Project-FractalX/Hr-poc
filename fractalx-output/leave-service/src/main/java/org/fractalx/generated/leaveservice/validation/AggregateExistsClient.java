package org.fractalx.generated.leaveservice.validation;

import org.fractalx.netscope.client.annotation.NetScopeClient;

/**
 * NetScope existence-check client for Aggregate.
 * Used by ReferenceValidator to enforce cross-service referential integrity.
 *
 * IMPORTANT: The remote service (aggregate-service) must expose an {@code exists(String id)}
 * method annotated with {@code @NetworkPublic} on its Aggregate bean.
 * Add this to the remote service if not already present:
 *
 * <pre>
 * {@code @NetworkPublic}
 * public boolean exists(String id) {
 *     return repository.existsById(id);
 * }
 * </pre>
 *
 * Auto-generated by FractalX — used by service: leave-service.
 */
@NetScopeClient(server = "aggregate-service", beanName = "Aggregate")
public interface AggregateExistsClient {

    /**
     * Returns true if an entity with this ID exists in Aggregate.
     */
    boolean exists(String id);
}
