package org.fractalx.generated.recruitmentservice.outbox;

import org.springframework.data.jpa.repository.JpaRepository;
import java.util.List;

/**
 * Repository for transactional outbox events.
 * Auto-generated by FractalX.
 */
public interface OutboxRepository extends JpaRepository<OutboxEvent, Long> {

    /** Returns unpublished events ordered by creation time (oldest first). */
    List<OutboxEvent> findByPublishedFalseOrderByCreatedAtAsc();

    /** Returns events that failed more than {@code maxRetries} times. */
    List<OutboxEvent> findByPublishedFalseAndRetryCountGreaterThan(int maxRetries);
}
