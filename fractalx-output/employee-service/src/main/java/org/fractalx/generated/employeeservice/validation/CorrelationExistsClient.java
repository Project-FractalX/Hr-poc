package org.fractalx.generated.employeeservice.validation;

import org.fractalx.netscope.client.annotation.NetScopeClient;

/**
 * NetScope existence-check client for Correlation.
 * Used by ReferenceValidator to enforce cross-service referential integrity.
 *
 * IMPORTANT: The remote service (correlation-service) must expose an {@code exists(String id)}
 * method annotated with {@code @NetworkPublic} on its Correlation bean.
 * Add this to the remote service if not already present:
 *
 * <pre>
 * {@code @NetworkPublic}
 * public boolean exists(String id) {
 *     return repository.existsById(id);
 * }
 * </pre>
 *
 * Auto-generated by FractalX — used by service: employee-service.
 */
@NetScopeClient(server = "correlation-service", beanName = "Correlation")
public interface CorrelationExistsClient {

    /**
     * Returns true if an entity with this ID exists in Correlation.
     */
    boolean exists(String id);
}
