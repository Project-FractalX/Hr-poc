spring:
  application:
    name: hr-monolith

  datasource:
#    url: jdbc:h2:mem:hr_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
    url: jdbc:postgresql://20.204.50.224:5432/hr_db?sslmode=require
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: wow123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

#  h2:
#    console:
#      enabled: true
#      path: /h2-console

server:
  port: 8080

# ─── FractalX runtime configuration ──────────────────────────────────────────
fractalx:
  enabled: true
  registry:
    url: http://localhost:8761
  saga:
    orchestrator:
      url: http://localhost:8099
  observability:
    tracing: true
    metrics: true
    logger-url: http://localhost:9099/api/logs

logging:
  level:
    com.example.hr: DEBUG
    org.fractalx:   INFO
    org.springframework.web: INFO
